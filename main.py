import logging
import uuid
from logging.handlers import MemoryHandler

# Logga kirish uchun funksiya
def logga_kirish(level, message, request_id):
    logging.log(level, f"[{request_id}] {message}")

# Logga kirish uchun funksiya (kengaytirilgan)
def logga_kirish_kengaytirilgan(level, message, request_id):
    logging.log(level, f"[{request_id}] {message}")

# Logga kirish uchun funksiya (xatolik)
def logga_kirish_xatolik(message, request_id):
    logging.error(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (xatolik, kengaytirilgan)
def logga_kirish_xatolik_kengaytirilgan(message, request_id):
    logging.error(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (harakat)
def logga_kirish_harakat(message, request_id):
    logging.info(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (harakat, kengaytirilgan)
def logga_kirish_harakat_kengaytirilgan(message, request_id):
    logging.info(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli)
def logga_kirish_muammoli(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request_id):
    logging.warning(f"[{request_id}] {message}")

# Logga kirish uchun funksiya (muammoli, kengaytirilgan)
def logga_kirish_muammoli_kengaytirilgan(message, request
